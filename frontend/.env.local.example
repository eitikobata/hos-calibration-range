# Copy this to .env.local (gitignored) and adjust if needed.
# Defaults to the local FastAPI dev server if this var isn't set at all -
# only needed once you're pointing the frontend at a deployed API Gateway URL.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
